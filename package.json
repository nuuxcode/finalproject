{
  "name": "my-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "pnpm --prefix client dev",
    "server": "pnpm --prefix server start",
    "dev": "pnpm run client & pnpm run server"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
